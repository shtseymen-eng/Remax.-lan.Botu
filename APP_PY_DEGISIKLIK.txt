V33 APP.PY DEĞİŞİKLİĞİ

Mevcut:
from .whatsapp_bot import WhatsAppBot

Yeni:
from .whatsapp_selenium import SeleniumWhatsAppBot

_home() sonunda mevcut:
self.wabot=WhatsAppBot(self.wa,lambda text:command_response(self.db.all(),text))

Yeni:
self.wabot=SeleniumWhatsAppBot(lambda text:command_response(self.db.all(),text))

Mevcut bot_start / bot_stop metodları aynı kalabilir; SeleniumWhatsAppBot start(), stop() ve status sinyalini aynı şekilde sağlar.

NOT:
Ana sayfadaki gömülü WhatsApp Web görsel olarak kalabilir ancak Max artık onu okumaz.
BOTU BAŞLAT düğmesi gerçek Chrome WhatsApp Web oturumunu açar.
İlk kullanımda QR bir kez okutulur; profil selenium-whatsapp-profile altında saklanır.
